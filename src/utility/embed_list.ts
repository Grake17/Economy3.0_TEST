// ===================================================
// Function List Embed
// ===================================================

// Import Discord Type
import { Message, MessageEmbed } from "discord.js";
// Import Table Interface


// Export Function
export default async function list(mgs: Message, list: string[]) {
    // New Embed
    const embed = new MessageEmbed();
}